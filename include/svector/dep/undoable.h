#ifndef UNDOABLE
#define UNDOABLE
#include"../svector.h"
#include<vector>

/*class UndoManager;
class UndoState;

class Undoable
{
	friend UndoManager;
public:
	Undoable(sci::shared_ptr<UndoManager> undoManager);
	const UndoState;
private:
	void undo();
	void redo();
	void setState(UndoState &undoState);
	shared_ptr<UndoManager> m_undoManager;
	std::vector<UndoState> m_undoList;
	UndoState *m_currentState;
};*/

#endif